#include <iostream>
int main() {
    bool a; std::cin >> a;
    if (a) {
        std::cout << "Leading the Way to the Future" << "\n";
    }
    else {
        std::cout << "YONSEI" << "\n";
    }
    return 0;
}